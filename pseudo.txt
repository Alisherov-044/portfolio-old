components -> ui -> sectionHeader

props: slug?: str title: str desc?: str btn?: reactNode

if slug:
    slug

title

if desc:
    desc

if btn:
    btn

style--

section props>

section>

section>
    sectionHeader>
        slug>slug<slug
        title>title<title
        desc>desc<desc
        btn>btn<btn
    sectionHeader>
section>